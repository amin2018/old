# Production environment specific variables

project_id = "prod-gcp-project-id" # Replace with the actual Production GCP project ID
region     = "production-region"   # Replace with the region for the Production environment
zone       = "production-zone"     # Replace with the zone for the Production environment

# Additional variables specific to the production environment can be added here
# For instance, machine types, resource sizes, and other configurations 
# that are suitable for a production-grade deployment.
